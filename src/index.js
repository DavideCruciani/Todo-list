import { navAnimation, navToggle } from '../nav'
import { createTaskBtn, createTaskModal, closeTask } from '../newtask'
import { Tasks, Task, addTask, TaskCreator } from '../alltasks';

navAnimation();

addTask("Compiti Italiano", "Dante pg256", "Alta")
addTask("Compiti Matematica", "Frazioni pg456", "Media")
